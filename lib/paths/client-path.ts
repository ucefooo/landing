const path = (root: string, sublink: string) => {
    return `${root}${sublink}`;
};

const APP_ROOT = '/';

export const APP_PATHS = {
    root: APP_ROOT,
    token: '#',
    aiRevenue: '#',
    aiLaunchpad: '#',
    connect: '#',
};
